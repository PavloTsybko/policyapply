# `@policyapply/contracts`

Provider-neutral identity and authorization decision contracts for PolicyApply.

This first slice intentionally contains no HTTP, database, identity-provider,
integration, or model SDK types. Identifiers are opaque, and request fields do
not grant authority by themselves.

The package remains private and unpublished while the public API is unstable.
